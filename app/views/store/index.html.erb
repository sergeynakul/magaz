<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h1>Catalog</h1>

<% @items.each do |item| %>
  <div>
    <h2><%= item.title %> - <%= number_to_currency(item.price) %>
    <%= button_to 'Add to Basket', basket_items_path(item_id: item) %></h2>
  </div>
<% end %>
